<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Edit Note</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>

<body class="bg-zinc-900 text-white min-h-screen">
    <div class="p-10 max-w-3xl mx-auto">
        <h1 class="text-3xl mb-6 font-bold">Edit Note</h1>

        <form action="/update/<%= filename %>" method="post">

            <input
                type="text"
                name="title"
                value="<%= oldTitle %>"
                class="block w-full mb-3 px-4 py-2 rounded bg-zinc-800"
                required
            >

            <textarea
                name="details"
                class="block w-full h-60 px-4 py-2 rounded bg-zinc-800 resize-none"
                required
            ><%= content %></textarea>

            <button class="mt-4 bg-green-600 px-6 py-2 rounded">
                Save Changes
            </button>
        </form>
    </div>
</body>
</html>
